module:
  name: module-c
  type: container
  allowPush: false
  services:
    - name: service-c
      endpoints:
        - path: /path-c
          port: http
      ports:
        - name: http
          containerPort: 8080
    - name: service-d
      endpoints:
        - path: /path-d
          port: http
      ports:
        - name: http
          containerPort: 8080
  build:
    dependencies:
      - module-b
