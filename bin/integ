#!/usr/bin/env bash

set -e

garden_root=$(cd `dirname $0` && cd $(git rev-parse --show-toplevel) && pwd)
garden_bin=${garden_root}/build/src/bin/garden.js

chmod +x ${garden_bin}

cd ${garden_root}/examples/hello-world

GARDEN_LOGGER_TYPE=basic ${garden_bin} build --force
