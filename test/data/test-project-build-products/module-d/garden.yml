module:
  name: module-d
  type: generic
  build:
    command: "echo D && mkdir -p build && touch build/d.txt"
    dependencies:
      - name: module-a
        copy:
          - a.txt
        copyDestination: a
      - name: module-b
        copy:
          - build/b1.txt
          - build/build_subdir
        copyDestination: b
