module:
  name: module-a
  type: generic
  services:
    service-a:
      command: echo ${local.env.TEST_VARIABLE}
  build:
    command: ${variables.service-a-build-command}
  test:
    unit:
      command: [echo, OK]
