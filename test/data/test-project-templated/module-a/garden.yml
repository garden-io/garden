module:
  name: module-a
  type: generic
  services:
    - name: service-a
      command: echo ${local.env.TEST_VARIABLE}
  build:
    command: ${variables.service-a-build-command}
  test:
    - name: unit
      command: [echo, OK]
