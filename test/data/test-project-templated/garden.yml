project:
  name: test-project-templated
  global:
    variables:
      some: ${local.env.TEST_VARIABLE}
      service-a-build-command: echo OK
  environments:
    local:
      providers:
        test-plugin: {}
