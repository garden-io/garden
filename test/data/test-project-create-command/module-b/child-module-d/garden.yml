module:
  name: child-module-d
  type: container
  description: Child module d container
  services:
    - name: child-module-d-service
      ports:
        - name: http
          containerPort: 8080
      endpoints:
        - paths:
            - /
          port: http
