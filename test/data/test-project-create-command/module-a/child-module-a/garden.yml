module:
  name: module-a
  type: container
  description: Module a container
  services:
    - name: module-a-service
      ports:
        - name: http
          containerPort: 8080
      endpoints:
        - paths:
            - /
          port: http
