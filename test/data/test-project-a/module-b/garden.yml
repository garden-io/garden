module:
  name: module-b
  type: generic
  services:
    - name: service-b
      dependencies:
        - service-a
  build:
    command: echo B
    dependencies:
      - module-a
  test:
    - name: unit
      command: [echo, OK]
