kind: Deploy
type: kubernetes
name: hello-world
spec:
  kustomize:
    path: .
  patchResources:
    - kind: Service
      name: my-service
      patch:
        metadata:
          namespace: preview-${local.env.GITHUB_REF} # <-- Namespace is set to something like preview-my-branch
