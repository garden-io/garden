module:
  description: Service dependant
  name: service-dependant
  type: container
  services:
    - name: service-dependant
      command: [npm, start]
      ports:
        - name: http
          containerPort: 8080
      ingresses:
        - path: /dependant
          port: http
      healthCheck:
        httpGet:
          path: /_ah/health
          port: http
      dependencies:
        - good-morning
