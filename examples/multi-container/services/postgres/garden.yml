module:
  description: postgres container
  type: container
  image: postgres:9.4
  services:
    db:
      volumes:
        - name: data
          containerPath: /db-data
      ports:
        db:
          containerPort: 5432
