description: Results presentation servicw
type: container
services:
  result:
    command: [nodemon, server.js]
    endpoints:
      - paths: [/]
        containerPort: 80
    ports:
      - name: ui
        protocol: TCP
        containerPort: 80
