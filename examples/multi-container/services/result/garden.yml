module:
  description: Results presentation service
  type: container
  services:
    result:
      command: [nodemon, server.js]
      endpoints:
        - paths: [/]
          port: ui
      ports:
        ui:
          protocol: TCP
          containerPort: 80
