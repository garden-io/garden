module:
  description: voting service
  type: container
  services:
    vote:
      command: [python, app.py]
      endpoints:
        - paths: [/vote/]
          port: interface
      ports:
        interface:
          protocol: TCP
          containerPort: 80
      dependencies:
        - redis
