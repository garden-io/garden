name: multi-container
environments:
  local:
    providers:
      docker:
        type: kubernetes
        context: docker-for-desktop
  dev:
    providers:
      docker:
        type: kubernetes
        context: my-dev-context
