module:
  description: redis service
  type: container
  name: redis
  image: redis:alpine
  services:
    - name: redis
      ports:
        - name: redis
          protocol: TCP
          containerPort: 6379
