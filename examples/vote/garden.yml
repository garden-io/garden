project:
  name: vote
  environments:
    - name: local
      providers:
        - name: local-kubernetes
          defaultHostname: vote.local.app.garden
          tlsCertificates:
            - name: dev
              secretRef:
                name: garden-tls
                namespace: default
