name: hello-world
environments:
  local:
    providers:
      docker:
        type: local-docker-swarm
  dev:
    providers:
      fargate:
        type: aws-fargate
      serverless:
        type: serverless
constants:
  message: Hello garden!
