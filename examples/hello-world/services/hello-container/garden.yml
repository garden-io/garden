description: Hello world container service
type: container
services:
  hello-container:
    command: [tail, -f, /dev/null]
    ports:
      - container: 80
build:
  dependencies:
    - hello-npm-package
