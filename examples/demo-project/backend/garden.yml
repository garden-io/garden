kind: Module
name: backend
description: Backend service container
type: container
tasks:
  - name: test
    command: [/bin/sh, -c]
    args: ["echo hello2"]
services:
  - name: backend
    ports:
      - name: http
        containerPort: 8080
        # Maps service:80 -> container:8080
        servicePort: 80
    ingresses:
      - path: /hello-backend
        port: http
    dependencies:
      - test