Produces the following error with `garden validate` (given you've changed the provider's context to something valid where you are testing):
```
Failed resolving one or more modules:

tmp: Error validating Module 'tmp': key .services[1] contains a duplicate value
```
