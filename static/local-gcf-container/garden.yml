module:
  description: Container for running Google Cloud Functions emulator
  type: container
  services:
    google-cloud-functions:
      endpoints:
        - paths: [/]
          port: http
      ports:
        http:
          protocol: TCP
          containerPort: 8010
      healthCheck:
        tcpPort: http
