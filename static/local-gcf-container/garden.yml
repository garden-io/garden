module:
  description: Container for running Google Cloud Functions emulator
  type: container
  services:
    google-cloud-functions:
      ports:
        http:
          protocol: TCP
          containerPort: 8010
      healthCheck:
        tcpPort: http
