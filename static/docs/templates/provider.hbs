---
title: "{{{frontmatterTitle}}}"
tocTitle: "`{{{name}}}`"
---

# {{{frontmatterTitle}}}

## Description

{{{docs}}}

Below is the full schema reference for the provider configuration..

The reference is divided into two sections. The [first section](#complete-yaml-schema) contains the complete YAML schema, and the [second section](#configuration-keys) describes each schema key.

## Complete YAML Schema

The values in the schema below are the default values.

```yaml
{{{yaml}}}
```
## Configuration Keys
{{{markdownReference}}}
{{#if moduleOutputsReference}}

## Outputs

The following keys are available via the `${providers.<provider-name>}` template string key for `{{{name}}}` providers.
{{{moduleOutputsReference}}}{{/if}}
