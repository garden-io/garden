# todo: remove this in project in 0.14
kind: Project
name: include-exclude
# This can't be automatically remapped to scalar `dotIgnoreFile: .customignore`, an error should be thrown
dotIgnoreFiles: [.customignore, .gitignore]
environments:
  - name: local
providers:
  - name: test-plugin
