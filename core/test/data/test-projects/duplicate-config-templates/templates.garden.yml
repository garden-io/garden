kind: ConfigTemplate
name: combo
modules:
  - type: test
    name: ${component-name}-${inputs.key}-test-a
  - type: test
    name: ${component-name}-${inputs.key}-test-b
---
kind: ConfigTemplate
name: combo
