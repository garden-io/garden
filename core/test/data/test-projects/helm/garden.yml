kind: Project
name: helm-test
providers:
  - name: local-kubernetes
    # Allow parallel tests in integ tests
    namespace: '${(local.env.INTEG == "1" && local.env.MOCHA_WORKER_ID) ? "container-${local.env.MOCHA_WORKER_ID}" : project.name}'
