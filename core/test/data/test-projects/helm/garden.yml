apiVersion: garden.io/v1
kind: Project
name: helm-test
environments:
  - name: local
providers:
  - name: local-kubernetes
    # Must be provided via --var namespaceName=xxx
    namespace: ${var.namespaceName}
