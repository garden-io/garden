module:
  name: child-module-a
  type: container
  description: Child Module a container
  services:
    - name: child-module-a-service
      ports:
        - name: http
          containerPort: 8080
      ingresses:
        - port: http
