module:
  name: module-a
  type: container
  services:
    - name: service-a
      ingresses:
        - path: /path-a
          port: http
      ports:
        - name: http
          containerPort: 8080
      dependencies:
        - workflow-a
  build:
    command: [echo, A]
  tasks:
    - name: workflow-a
      command: [echo, A]
      dependencies:
        - workflow-c