module:
  name: module-a
  repositoryUrl: https://my-git-server.com/my-repo.git#master
  type: test
  services:
    - name: service-a
  build:
    command: [echo, A]
  tests:
    - name: unit
      command: [echo, OK]
