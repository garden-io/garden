module:
  name: module-a
  repositoryUrl: https://my-git-server.com/module-a.git#master
  type: test
  services:
    - name: service-a
  build:
    command: [echo, A]
  tests:
    - name: unit
      command: [echo, OK]
