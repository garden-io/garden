module:
  name: module-c
  type: test
  services:
    - name: service-c
  build:
    dependencies:
      - module-b
  tests:
    - name: unit
      command: [echo, OK]
  tasks:
    - name: task-c
      command: [echo, OK]
