module:
  name: module-a
  type: test
  services:
    - name: service-a
  build:
    command: [echo, A]
  tests:
    - name: unit
      command: [echo, OK]
  tasks:
    - name: workflow-a
      command: [echo, OK]